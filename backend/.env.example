# Flask backend configuration
# Copy to `.env` and update with your secrets before running locally or in production.

# Required secrets
JWT_SECRET_KEY=replace-with-strong-random-string
ADMIN_PASSWORD=replace-with-admin-password

# Frontend origin used for CORS
# For production deployments, set this to your deployed frontend URL (e.g. https://your-app.vercel.app)
FRONTEND_ORIGIN=http://localhost:3000

# Primary database connection (works for Railway/Render/Heroku style URLs)
DATABASE_URL=postgresql://username:password@localhost:5432/boilerfuel

# Optional: discrete Postgres settings. Leave commented unless your provider uses them.
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_USER=username
# POSTGRES_PASSWORD=password
# POSTGRES_DB=boilerfuel

# Optional SSL mode for managed Postgres (Railway/Neon/etc.). Use "require" when needed.
# DATABASE_SSLMODE=require
# PGSSLMODE=require
