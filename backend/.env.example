# Database Configuration
# For local development, use a local PostgreSQL database
DATABASE_URL=postgresql://username:password@localhost/boilerfuel

# Or use discrete parts (Railway and other providers may set these)
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_USER=username
# POSTGRES_PASSWORD=password
# POSTGRES_DB=boilerfuel

# JWT Secret Key - CHANGE THIS IN PRODUCTION!
JWT_SECRET_KEY=your-secret-key-here-change-in-production

# Optional: SSL mode for database connection
# DATABASE_SSLMODE=require
# PGSSLMODE=require
