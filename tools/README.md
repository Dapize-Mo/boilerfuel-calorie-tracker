# Tools Directory

Utility scripts for maintaining the BoilerFuel application.

## maintenance/

- **`auto_sync_menus.py`** - Daily menu synchronization

### Usage

```bash
python tools/maintenance/auto_sync_menus.py --days 7
```

Requires the virtual environment to be activated and `DATABASE_URL` to be set.
